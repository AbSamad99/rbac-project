# rbacserver

Application entry point.

| Class | Description |
|---|---|
| `RbacServerApplication` | Main Spring Boot class — `@SpringBootApplication` with JPA repository, component, and entity scanning configured for `com.syed.code` |
